---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: django-ref-app
  namespace: ${K8S_NAMESPACE}
spec:
  selector:
    matchLabels:
      app: django-ref-app
  endpoints:
  - port: metrics
    interval: 15s
    
