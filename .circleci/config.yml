version: 2
jobs:
  build:
    working_directory: /app
    docker:
      - image: ministryofjustice/cloud-platform-tools
    steps:
      - run:
          name: just testing
          command: |
            echo $AWS_ACCESS_KEY_ID_DEMO	
